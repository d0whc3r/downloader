diff --git a/dist/plugin/index.cjs b/dist/plugin/index.cjs
index bb3f95180da9eabfac17043c913a44375c97683d..758060a16ea8e02d812d115bb57204dd364ec73d 100644
--- a/dist/plugin/index.cjs
+++ b/dist/plugin/index.cjs
@@ -79,8 +79,10 @@ var generateRouteTypes = async (options) => {
       if (param.length) {
         params.push(`'/${path3}': { ${param.map((p) => p.replace(/:(.+)(\?)?/, "$1$2:") + " string").join("; ")} }`);
       }
-      if (path3.includes("*"))
+      if (path3.includes("*")) {
+        params.push(`'/${path3}': { '*': string }`);
         return "/" + path3.replace(/\*/g, "${string}");
+      }
       return path3.length > 1 ? `/${path3}` : path3;
     }
   });

diff --git a/node_modules/@spaceribs/nx-web-ext/src/generators/application/generator.js b/node_modules/@spaceribs/nx-web-ext/src/generators/application/generator.js
index c8e3ad0..97d5460 100644
--- a/node_modules/@spaceribs/nx-web-ext/src/generators/application/generator.js
+++ b/node_modules/@spaceribs/nx-web-ext/src/generators/application/generator.js
@@ -4,8 +4,7 @@ exports.addFiles = void 0;
 const tslib_1 = require("tslib");
 const devkit_1 = require("@nx/devkit");
 const path = require("path");
-const angular_1 = require("./sub-generators/angular");
-const react_1 = require("./sub-generators/react");
+
 /**
  * Take the raw configuration and transform/decorate it for use.
  * @param options - Raw options passed from the generator.
@@ -36,10 +35,10 @@ function default_1(tree, options) {
         const normalizedOptions = normalizeOptions(options);
         switch (normalizedOptions.framework) {
             case 'angular':
-                yield (0, angular_1.angularApp)(tree, normalizedOptions);
+                yield Promise.resolve().then(() => require('./sub-generators/angular')).then(({ angularApp }) => angularApp(tree, normalizedOptions));
                 break;
             case 'react':
-                yield (0, react_1.reactApp)(tree, normalizedOptions);
+                yield Promise.resolve().then(() => require('./sub-generators/react')).then(({ reactApp }) => reactApp(tree, normalizedOptions));
                 break;
             default:
                 throw new Error('This application target is not supported.');
